const empModule = require('../class_emp');
describe('Emp class UT',() => {
    let empObj;
    it('Check Name',() => {
         empObj = new empModule.Emp()
        // expect(emp.fullName).toBe('Rwik')
    });
});